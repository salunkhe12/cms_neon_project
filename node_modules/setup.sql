CREATE TABLE articles (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255),
    content TEXT,
    category_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255)
);

INSERT INTO categories (name) VALUES ('Technology'), ('Health');
INSERT INTO articles (title, content, category_id) 
VALUES ('PostgreSQL Integration', 'Learn to use PostgreSQL', 1),
       ('Healthy Living Tips', 'Tips for a healthy lifestyle', 2);
